// import { CreatePageArgs } from "gatsby";

export function onCreatePage() {
  // args: CreatePageArgs
  /*
    // Create dynamic single-page application routes here
  
    if (page.path.startsWith(ROUTE.path)) {
      const { createPage } = actions;

      page.matchPath = `${ROUTE.path}/*`;

      createPage(page);
    }
  */
}
